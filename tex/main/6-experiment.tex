\section{Experiments}
\label{sec:experiments}

This section reports controlled checks for two structural claims of REINFORCE Pro Max: (i)~the leave-one-out baseline and adaptive asymmetric normalization have the stated advantage-statistics properties; and (ii)~prefix-level masking behaves as predicted under the deviation patterns analyzed in \Cref{thm:prefix-tighter}. The checks are deliberately synthetic and do not constitute a language-model benchmark. The executable protocol is in \path{reinforce_pro_max/structural_experiment.py}; its fixed-seed output is saved in \path{reinforce_pro_max/structural_experiment_20260918.json}.

\subsection{Experimental Setup}

We specify a simulation protocol with $n=8$ samples per prompt. Rewards are drawn from a skewed mixture that mimics sparse-success reasoning settings: with probability $p=0.15$ a sample is drawn from a ``success'' cluster centered near $1$, otherwise from a ``failure'' cluster centered near $0$. We use bounded perturbations $\eta_i\sim\mathrm{Unif}[-0.10,0.10]$ around these centers and clip the resulting rewards to $[-0.10,1.10]$, so the simulation satisfies the bounded-reward assumption used by the finite-sample guarantees. To reflect token-level weighting effects after expansion (\Cref{sec:token-expand}), we assign longer sequences to lower-quality samples (failure trajectories), setting lengths to
\[
    T_i = \begin{cases}
        256 + u_i, & \text{success sample},\\
        512 + u_i, & \text{failure sample},
    \end{cases}
    \qquad u_i \sim \mathrm{Unif}\{-32,\ldots,32\}.
\]

For IS masking, we use the acceptance interval $[1-\epslow,1+\epshigh]=[0.9,1.1]$ unless otherwise stated (i.e., $\epslow=\epshigh=0.1$).
We also define the corresponding log-thresholds
\[
    \tau_+ := \log(1+\epshigh),
    \qquad
    \tau_- := -\log(1-\epslow),
\]
so that $\rho_t \in [1-\epslow,1+\epshigh]$ is equivalent to $\log\rho_t \in [-\tau_-,\tau_+]$.
Finally, we construct synthetic log-ratio patterns with $T=32$ to directly instantiate the three regimes in \Cref{thm:prefix-tighter}.

\subsection{Baselines}

\textbf{Baselines and normalization.} We compare the mean baseline (REINFORCE++/GRPO-style) to the leave-one-out (RLOO) baseline (\Cref{def:rloo}), and we compare global normalization $(A-\mu)/\sigma$ to adaptive asymmetric normalization (\Cref{def:adaptive-norm}).

\textbf{IS masking.} We compare token-level masking (IcePop), sequence-level masking (GSPO-style, based on $\rhoseq$), and prefix-level
masking based on the prefix proxy in \Cref{def:prefix-is}.

\subsection{Metrics}

\textbf{Baseline self-correlation.} We measure the pooled Pearson correlation over all $8{,}000$ sampled responses, $\mathrm{corr}(r_i, b_i)$ between each sample reward $r_i$ and its baseline $b_i$. A
correlated baseline can couple a sample with its own control variate, complicating the clean second-moment analysis enabled by RLOO (see
\Cref{app:rloo-proof}).

\textbf{Sign flip rate under normalization.} After token expansion, we compute the token-weighted fraction of non-zero tokens whose normalized advantage changes sign, i.e., $\mathbb{I}[\mathrm{sign}(\hat{A}_{i,t}) \neq \mathrm{sign}(A_{i,t})]$. Sign flips are undesirable because they can turn punishment into reinforcement (or vice versa).

\textbf{Masked-token coverage.} For each synthetic log-ratio pattern, we report the number of masked tokens and the first/last masked
positions for IcePop, sequence-level IS masking, and prefix masking.

\subsection{Formal and Deterministic Checks}

\begin{table}[ht]
\centering
\caption{Deterministic properties checked by the Lean formalization.}
\renewcommand{\arraystretch}{1.2}
\small
\begin{tabular}{lcc}
\toprule
\textbf{Property} & \textbf{Mean / global} & \textbf{RLOO / adaptive} \\
\midrule
Raw baseline scaling & $(n-1)/n$ & $1$ \\
Within-group shaped-reward sum & $0$ & $0$ \\
Sign preservation under normalization & not guaranteed & exact for non-zero active advantages \\
\bottomrule
\end{tabular}
\end{table}

\noindent The first row describes the raw estimators before normalization,
using the exact deterministic proportionality between the mean-baseline and leave-one-out signals (
\path{rloo_scaling}); the second row follows from the mean-baseline and RLOO
conservation identities (\path{mean_baseline_shaped_sum_zero} and
\path{rloo_shaped_sum_zero}); and the third row follows from positive sign-dependent scaling (
\path{positive_scaling_preserves_sign}). The sign statement concerns active
tokens; padding positions are removed by the action mask before normalization.
The zero-sum row concerns response-level rewards before token expansion or
normalization; it holds for both baselines. Unequal response lengths can
produce a nonzero token-weighted mean for either baseline.
Finite-iid population baseline moments and the RLOO score cross-moment are
also checked in \path{formal/RLOOMoments.lean}; these probability identities
are distinct from numerical simulation results.

\begin{table}[ht]
\centering
\caption{Fixed-seed structural simulation ($1{,}000$ prompt groups, $n=8$). These are synthetic checks, not RLVR benchmark results.}
\renewcommand{\arraystretch}{1.15}
\small
\begin{tabular}{lr}
\toprule
\textbf{Measured quantity} & \textbf{Value} \\
\midrule
Mean-baseline/reward correlation & $0.3547$ \\
RLOO-baseline/reward correlation & $0.00235$ \\
Adaptive-normalization sign-flip rate & $0$ \\
Global-normalization sign-flip rate & $0.0465$ \\
Adaptive non-zero-token mean & $1.8\times10^{-9}$ \\
Adaptive non-zero-token population variance & $1.0000$ \\
Maximum absolute RLOO group-sum residual & $6.3\times10^{-7}$ \\
\bottomrule
\end{tabular}
\end{table}

The correlations use the ordinary within-group mean baseline and the
leave-one-out baseline on the same sampled rewards. The latter is nearly
uncorrelated with the held-out reward in this iid simulation, whereas the
ordinary mean baseline includes that reward by construction. The adaptive
statistics are computed on non-zero active tokens, matching the ideal
normalization definition; implementation safeguards are excluded from this simulation and their exact-real formulas are formalized as residual bounds. These measurements do not validate the full training implementation. The sign-flip denominator contains $3{,}785{,}796$ nonzero active tokens; adaptive normalization flips $0$ and global normalization flips $176{,}197$. The small nonzero moment and group-sum residuals reflect floating-point arithmetic, not exact mathematical equalities. These measurements
support the structural claims only for this synthetic protocol.

Using the same ideal asymmetric normalization for both baselines removes
their common scale and produces identical coefficients, as proved in
\Cref{prop:normalization-scale-invariance}. A baseline ablation must
therefore record the normalization rule and active numerical safeguards.
For exact two-level rewards, \Cref{prop:binary-rloo-normalization} gives
the normalized amplitudes $+\sqrt{N/P}$ and $-\sqrt{P/N}$ and the factor
ratio $\bar T_+/\bar T_-$. These formulas provide deterministic checks
for reward-frequency and response-length sweeps; they do not constitute
measured improvements on an LLM benchmark.
For exact binary rewards, token-weighted standardization within the same
prompt is an equivalent ideal operation
(\Cref{cor:binary-token-standardization}), so it cannot be treated as a
different ideal normalization baseline. The proposed perturbed-reward
protocol is multivalued; the general sign-preservation comparison can differ
in that setting. The finite RLOO example in
\Cref{par:multivalued-sign-example} verifies such a difference exactly.

The optional uniform-scale ablation must also record the reward zero point.
\Cref{prop:uniform-population} gives its added expected score term for
binary rewards, and Eq.~\eqref{eq:uniform-offset-counterexample} provides
an exact two-response check: shifting $0/1$ rewards to $3/4$ preserves
their ordering and reward gradient but can reverse the update with uniform
scale enabled. All-zero reward groups remain zero in either mode.

\begin{table}[ht]
\centering
\caption{Masking behavior under synthetic deviation patterns. We use $T=32$ and $[1-\epslow,1+\epshigh]=[0.9,1.1]$. We report the number of masked tokens and the masked position range.}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{lccc}
\toprule
\textbf{Pattern} & \textbf{IcePop (token)} & \textbf{Seq (GSPO-style)} & \textbf{Prefix (ours)} \\
\midrule
Early ($\log\rho_1{=}8\tau_+$; else $0$) & $1$ ($[1,1]$) & $0$ (---) & $7$ ($[1,7]$) \\
Late ($\log\rho_{24}{=}32\tau_+$; else $0$) & $1$ ($[24,24]$) & $0$ (---) & $8$ ($[24,31]$) \\
Drift ($\log\rho_t{=}2\tau_+,\ \forall t$) & $32$ ($[1,32]$) & $32$ ($[1,32]$) & $32$ ($[1,32]$) \\
\bottomrule
\end{tabular}
\end{table}

\noindent The early and late patterns instantiate \Cref{thm:prefix-tighter}(a,b) in the simplest form with $\varepsilon=0$ (all non-spike
tokens satisfy $\rho_t=1$ and hence lie within the trust-region interval).
\par
\noindent\textbf{Early deviation.} IcePop reacts only to the local outlier at $t=1$ and therefore masks a single token. Sequence-level
masking aggregates over the full horizon and dilutes the violation. Prefix masking, in contrast, propagates the early mismatch forward by
masking a short prefix:
\[
    \log\rhoprefix{t} = \frac{1}{t}\log\rho_1 = \frac{8}{t}\tau_+,
    \qquad
    \log\rhoseq = \frac{1}{T}\log\rho_1 = \frac{1}{4}\tau_+,
    \qquad (T=32).
\]
Thus $\log\rhoprefix{t}>\tau_+$ for $t\in\{1,\ldots,7\}$, while $\log\rhoseq$ remains in-bound.
\par
\noindent\textbf{Late deviation.} For a single spike at $t^*=24$, prefix masking is strictly suffix-local (it can only start at $t\ge t^*$),
while sequence-level masking remains all-or-nothing. With $\log\rho_{24}=32\tau_+$ and $\log\rho_t=0$ otherwise, the sequence statistic
satisfies $\log\rhoseq=\tau_+$ and is accepted, but the prefix average exceeds $\tau_+$ for $t\in\{24,\ldots,31\}$, yielding a localized
masked suffix. These behaviors align with the regimes in \Cref{thm:prefix-tighter}. The Lean certificates \path{early_table_counts}, \path{late_table_counts}, and \path{drift_table_counts} verify all three rows for any positive upper log-threshold and nonpositive lower log-threshold. The position certificates use zero-based indices, corresponding to the one-based positions in the table.

\subsection{Ablation Studies}

\textbf{Without adaptive normalization.} Global normalization can flip the sign of token-expanded advantages when token-length weighting makes the token-level mean non-zero, while adaptive normalization preserves sign by using separate positive/negative scalings (\Cref{par:adv-sign-preservation}).

\textbf{Without prefix masking.} Token-level masking only reacts to per-token outliers and may leave future tokens unfiltered even when the context has already drifted; prefix masking filters based on the prefix statistic, which is aligned with the causal accumulation structure (\Cref{rem:coupling}, \Cref{thm:prefix-tighter}).

\subsection{Sensitivity to Thresholds}

Masking behavior depends on $[1-\epslow,1+\epshigh]$. Stricter thresholds can cause sequence-level IS masking to reject an entire trajectory,
while prefix masking remains localized in position. For example, in the late-deviation pattern above, the sequence statistic satisfies
$\rhoseq=1+\epshigh=1.1$ under the default thresholds. If we instead use the stricter symmetric interval $[1-\epslow,1+\epshigh]=[0.95,1.05]$
(i.e., $\epslow=\epshigh=0.05$), then $\rhoseq>1.05$ and the sequence-level mask rejects all $32$ tokens, while prefix masking still rejects only
suffix positions starting at $t=24$.

\subsection{Reproducibility Checklist}

\begin{itemize}
    \item The Monte Carlo protocol uses $1{,}000$ groups, $n=8$ samples per prompt, and seed $20260918$, with CPU PyTorch 2.4.0 float32 arithmetic and three intra-op threads. Correlations pool responses; sign-flip rates divide by the number of active tokens with nonzero raw advantage. Normalization is performed within each prompt group; reported moments pool its expanded tokens and use population variance.
    \item Reward distribution: mixture with $p=0.15$, bounded perturbation $\eta_i\sim\mathrm{Unif}[-0.10,0.10]$, and clipping to $[-0.10,1.10]$.
    \item Token lengths: success $T_i = 256 + u_i$, failure $T_i = 512 + u_i$, $u_i \sim \mathrm{Unif}\{-32,\ldots,32\}$.
    \item Thresholds: default $(\epslow,\epshigh)=(0.1,0.1)$ (i.e., $[1-\epslow,1+\epshigh]=[0.9,1.1]$); sensitivity example $(\epslow,\epshigh)=(0.05,0.05)$ (i.e., $[0.95,1.05]$).
    \item The deterministic masking table can be reproduced directly from the formulas in
    \Cref{thm:prefix-tighter}. Exact-real counts and rejection positions are also checked in
    \path{formal/StructuralExamples.lean}; the executable script reproduces both tables.
\end{itemize}
